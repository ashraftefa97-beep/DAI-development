import React from 'react';
import ReactDOM from 'react-dom/client';
import GithubApp from './GithubApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode><GithubApp/></React.StrictMode>
);
